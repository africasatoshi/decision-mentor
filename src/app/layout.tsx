import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Decision Mentor - AI-Powered Decision Making Assistant",
  description: "Transform complex decisions into clear actions with expert frameworks and AI technology.",
  keywords: "decision making, AI assistant, business decisions, career decisions, decision framework",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
